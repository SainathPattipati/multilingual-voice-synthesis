"""Voice cloning."""

class VoiceCloner:
    """Clone voices from reference audio."""
    
    def clone(self, reference_audio_path: str) -> str:
        """Clone voice."""
        return "voice_embedding_id"
